#include <iostream>
#include <vector>

using namespace std;

int minOperations(vector<int>& arr, int k) {
    int n = arr.size();

    for(int i=0 ;i <n ;i++){
        int mini = f()
    }
}



// 3 5 5 2 1 1 1

int main() {
    int n, k;
    cin >> n >> k;

    vector<int> arr(n);

    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }

    int result = minOperations(arr, k);

    cout << result << endl;

    return 0;
}
