#include <bits/stdc++.h>
#define ll long long
#define endl "\n"
using namespace std;


int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t,n,l;
    cin>>t;
    while(t--){
        cin>>n>>l;
        vector<int> lamp(n);
        for(int i=0;i<n;i++){
            cin>>lamp(i);
        }
        sort(lamp.begin(),lamp.end());
        
    }
    return 0;
}
